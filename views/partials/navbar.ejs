<nav class="navbar sticky-top navbar-expand-lg mx-2 bg-color-dark">
  <div class="container-fluid">
    <a class="navbar-brand text-light" href="/">Pu-erh Catalogue</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div
      class="collapse navbar-collapse justify-content-end"
      id="navbarNavAltMarkup"
    >
      <% if (currentUser) { %>
      <div class="navbar-nav mx-1">
        <a class="nav-link active text-light" href="/tea">Browse teas</a>
        <a class="nav-link text-light" href="/tea/new">New tea</a>
        <a class="nav-link text-light" href="/tea/collection">Your collection</a>
      </div>
      <% } %>
      <div class="navbar-nav mx-1">
        <% if(!currentUser) {%>
        <a class="nav-link text-light" href="/login">Sign in</a>
        <%} else {%>
        <a class="nav-link text-light" href="/logout">Logout</a>
        <%}%>
      </div>
    </div>
  </div>
</nav>
